// Layout completo do componente Dashboard
// (Esse trecho incluiria o conteúdo completo que está salvo no canvas)

// Substitua a linha: const token = "2e3d2ff5-..." por:
const token = import.meta.env.VITE_CLICKSIGN_TOKEN;

// E no envio do e-mail Brevo, substitua:
"api-key": import.meta.env.VITE_BREVO_API_KEY